# Steel Transmittal Database

Offline web application that behaves like a small database system using LocalStorage.

- Paste Excel-like data
- Smart parsing into a single record
- LocalStorage database
- Search, filter, sort, edit, delete
- Excel export/import via SheetJS
- Backup/restore JSON
- Dark/Light mode, responsive UI

Open `index.html` in a browser. No backend required.
